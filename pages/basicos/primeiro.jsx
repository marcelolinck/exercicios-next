//Quando criar o arquivo dentro da pagina pages, o next interpreta como sendo o caminho para ser acessado via url, nesse exemplo ele criou a url /primeiro

export default function primeiro() {
    return "Primeiro V2"
}

